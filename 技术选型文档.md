# 《趣狗乐学》小程序 技术选型文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.1（变更：组件库 TDesign → NutUI，见文末变更记录） |
| 编写日期 | 2026-08-29 |
| 依据文档 | 《趣狗乐学》小程序产品需求文档 PRD v1.0 |
| 已确认决策 | 后端采用微信云开发；前端采用 Taro（React 语法） |

---

## 1. 选型结论总表

| 层级 | 选型 | 版本/说明 |
| --- | --- | --- |
| 前端框架 | Taro + React | Taro 4.x，React 18，TypeScript |
| 状态管理 | Zustand | 轻量，适合中小型 React 应用 |
| 组件库 | NutUI-React-Taro（@nutui/nutui-react-taro） | 京东官方 Taro React 组件库 v3.0.20；原选 TDesign 因无可用 Taro React 包而更换（见文末变更记录） |
| 动画/游戏表现 | CSS 动画 + Lottie（lottie-miniprogram） | 花园生长、庆祝粒子等 |
| 后端 | 微信云开发 CloudBase | 云函数 + 云数据库 + 云存储 |
| 云函数运行时 | Node.js 18 LTS | TypeScript 编写 |
| PDF 生成 | pdfkit（云函数内生成） | 内置中文字体子集，A4 排版 |
| 语音朗读 TTS | 微信同声传译插件（WechatSI） | 官方免费插件，拼音/古诗/英语朗读 |
| 登录体系 | 云开发免鉴权 openid | 无需自建账号体系 |
| 消息推送 | 微信订阅消息 + 云函数定时触发器 | 每日提醒（FR-B3） |
| 内容安全 | 微信云调用 msgSecCheck / imgSecCheck | AI 内容先审核后入库（P1） |
| AI 能力（P1） | 云开发 AI 网关（小程序 AI 成长计划） | 生文模型动态出题、教材识别（OCR + 模型） |
| 图像生成 | 腾讯云混元生图 API（备选：智谱 CogView） | 题目插图、题卡素材、花园作物等童趣风素材批量生成 |
| AI 内容生产模式 | 离线批量生成 → 机审+人工审核 → 入内容库 | 孩子使用时只读内容库，不做实时生成（见 4.5） |
| 埋点 | 云数据库事件表自建 + 微信官方数据分析 | MVP 自建轻量埋点，验证 H1–H4 |
| 单元测试 | Jest | 云函数与规则引擎出题逻辑 |
| 自动化测试 | miniprogram-automator | 关键路径回归（激活路径、打卡、打印） |
| 包管理/规范 | pnpm + ESLint + Prettier | 统一代码风格 |

## 2. 前端选型：Taro + React

**决策：Taro 4.x + React 18 + TypeScript。**（已确认）

选型理由：

- React 生态成熟，团队如后续扩展 H5 端（P1 分享报告落地页）可复用代码；
- TypeScript 对题库结构、难度档位、报告数据等强类型场景收益明显；
- Taro 4.x 已稳定支持微信小程序主要新能力（订阅消息、同声传译插件、分包加载）。

注意事项（选型即接受的成本）：

- 存在编译层，小程序端疑难问题排查成本高于原生；
- 游戏化页面（口算闯关、翻翻乐、花园动画）以自绘组件 + Lottie 为主，不依赖组件库；
- 首屏 < 2 秒（PRD 8.1）需配合分包加载：游戏玩法、打印小铺各自分包。

备选方案回顾：原生小程序（性能最优、无编译层，但无法复用到 H5）；uni-app（Vue 语法，团队技术栈不符）。两者已排除，原因如上。

## 3. 后端选型：微信云开发 CloudBase

**决策：云函数 + 云数据库 + 云存储，免自建服务器。**（已确认）

选型理由：

- PRD 8.3 已计划报名「小程序 AI 成长计划」，可领取免费云开发环境与 Token 额度，MVP 阶段云资源成本趋近于零；
- 免运维、免域名备案、免 HTTPS 证书，云调用直接获得 openid 免鉴权与内容安全接口；
- 订阅消息推送用云函数定时触发器即可实现每日 19:30 提醒（FR-B3），无需额外服务；
- 免费无广告的 MVP 策略下，成本最小化是硬约束。

主要映射关系：

| PRD 需求 | 云开发实现 |
| --- | --- |
| FR-A1 今日任务 / 规则引擎出题 | 题库存云数据库集合，云函数按龄段与难度档抽题，题包下发前端缓存（支持离线作答，成绩恢复联网补传） |
| FR-A2 成长花园状态 | 云数据库用户档案文档持久化，答对先本地播放动画（<300ms）再异步落库 |
| FR-B1/B2 报告与打卡 | 按日聚合文档 + 打卡月历集合，streak 由打卡记录计算 |
| FR-B5 防沉迷 | 前端计时（前台活跃口径）+ 云端校验兜底 |
| FR-C3 PDF 生成 | 云函数 pdfkit 渲染 A4 PDF，存云存储，返回临时链接 |
| FR-D1/D2/D3、模块 F（P1/P2） | 云开发 AI 网关调生文模型，OCR 用微信 OCR 云调用 + 模型提炼知识点 |

注意事项（选型即接受的成本）：

- 被锁定在腾讯体系，数据迁移成本高（MVP 阶段可接受，出现强迁移需求时再评估）；
- 云数据库复杂聚合查询能力有限，报告类查询靠合理的集合设计（按日预聚合）规避；
- 云函数冷启动需关注（PDF 生成等函数设置更高内存并预留预热策略）。

备选方案回顾：自建 Node.js/Spring Boot + MySQL + 轻量服务器（可控性强但需服务器、备案、运维，MVP 成本明显增加，已排除）；混合架构（架构复杂，MVP 不需要，P1 如遇 AI 重功能瓶颈再评估）。

## 4. 关键专项选型

### 4.1 PDF 生成（打印小铺，P0 核心）

**决策：云函数内用 pdfkit 生成，中文字体打包进函数（思源黑体子集，仅含题卡用字）。**

- 输出 A4、裁切线、可爱插图装饰（插图素材云存储读取），满足生成 < 5 秒、文件 < 5MB（PRD 8.1）；
- 插图素材来源：AI 生成原创童趣风图片，先审核后入库（FR-C3 合规约束），生成期提前批量制作存云存储，不随 PDF 实时生成。

备选：前端 Canvas 导出图片（无法产出真 PDF、不可转发文件，不符合 FR-C4）；puppeteer 服务端渲染（云函数环境重、内存大，已排除）。

### 4.2 语音朗读（翻翻乐配对发音，P0）

**决策：微信同声传译插件（WechatSI）。** 官方免费，支持中文/英语 TTS，满足拼音、古诗、英语卡片朗读（FR-G2）。备选云厂商付费 TTS（音质更好但按调用量计费，与免费策略冲突，已排除；P1 如需童声音色再评估）。

### 4.3 AI 能力（P1，P0 全部规则引擎实现）

- P0：规则引擎出题（预设题库 + 难度档位 + 升降档规则），纯云函数实现，无 AI 依赖；
- P1：报名「小程序 AI 成长计划」领取免费额度，经云开发 AI 网关调用生文模型实现 FR-D1 自适应出题、模块 E 认知内容制作、模块 F 教材识别与同步练习；
- 硬性合规：所有 AI 生成内容先审核后入库（FR-D/模块 E 约束）；教材图片仅即时解析不长期存储（FR-F 约束）；不提供开放式对话（PRD 8.2）。

### 4.4 内容安全与合规（P0 即接入）

- UGC 与 AI 内容：云调用 `msgSecCheck`（文本）/ `imgSecCheck`（图片），P1 AI 内容入库前机审 + 人工抽审；
- 未成年人隐私：档案仅存年龄/年级与学习进度，不采集姓名/照片/定位（PRD 8.2），云数据库不建冗余采集字段，从数据模型层约束。

### 4.5 AI 图文内容生产管线（本产品核心内容模式）

本产品的学习内容以「AI 生成的图文并茂内容」为主，而非传统纯文字题库。AI 是内容工厂，不是运行时服务：

**P0 / MVP：离线批量预生成，运行时只读内容库。**

```
内容生产管线（离线，上架前执行）：
生文模型批量出题（含题目文案/解析/标签）
  → 生图模型按统一风格 Prompt 生成插图
  → msgSecCheck / imgSecCheck 机审
  → 人工审核
  → 写入内容库集合（题目 + 插图 URL + 龄段/难度/知识点标签）
运行时（孩子使用时）：
云函数按龄段/难度档从内容库抽题 → 前端 CDN 加载插图
```

采用此模式而非实时生成的三个硬约束：

1. **延迟**：生图单张 5–30 秒，无法满足答题反馈 <300ms（FR-A2）与首屏 <2 秒（PRD 8.1）；
2. **安全**：实时生成内容未经审核即展示给 4–8 岁儿童，违反 PRD 8.2 与小程序审核要求；
3. **成本**：按张计费，孩子每题都有插图，实时生成的月成本不可控。

**图像生成模型：腾讯云混元生图 API**（与云开发同一腾讯账单与生态，接口稳定；备选智谱 CogView-3，价格更低可作批量主力）。所有素材统一「可爱童趣扁平风」Prompt 模板 + 固定负面词，保证跨模块视觉一致性（花园作物、翻翻乐卡片、题卡插图同风格）；生成结果全部先审核后入库。

**与 PRD 的关系**：P0「规则引擎出题」的题库制作方式由此管线承担（AI 批量生成题目与插图，代替人工编写与手绘）；P1 实时生成仅用于教材同步（FR-F2/F3，用户上传后有「识别中」过渡页，可容忍等待，结果仍先审核后呈现）。

**成本与配额控制**：内容库按「每龄段 × 每科目 × 每难度档」规划最小题包（首批估算：2 龄段 × 3–4 科目 × 3 档 × 100 题 ≈ 2000 题量级，插图按主题批量生成复用），生成脚本走云函数 + 定时触发器分批执行，失败重试不重复计费。

## 5. 工程与质量

- **代码结构**：pnpm monorepo（`client` Taro 前端 + `cloud/functions` 云函数），TypeScript 全量；
- **规范**：ESLint + Prettier + Git 提交遵循 Conventional Commits；
- **单元测试**：Jest 覆盖规则引擎出题（难度升降档 FR-D1）、打卡 streak 计算（FR-B2）、打印参数校验（FR-C2）等纯逻辑；
- **自动化回归**：miniprogram-automator 覆盖新用户激活路径（FR-N1）、每日闭环、打印生成三条核心流程；
- **发布**：miniprogram-ci 配置体验版自动构建；云函数通过 CloudBase CLI 部署，区分 dev / prod 环境。

## 6. 选型约束与风险

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| Taro 编译层疑难问题 | 游戏化页面调试成本 | 核心游戏页保持组件简单、动画用 Lottie/CSS，避免复杂框架特性 |
| 云开发免费额度上限 | P1 AI 调用与存储增长 | 上线前明确额度水位监控，超限前评估付费档或混合方案 |
| 云函数包体（中文字体） | PDF 函数体积偏大 | 仅打包题卡所需字体子集（<2MB），放大函数内存配置 |
| 生图风格不一致 / 废片率高 | 素材返工，管线成本上升 | 统一风格 Prompt 模板 + 固定负面词，按主题批量生成复用，机审前先做脚本化质量初筛 |
| 内容库审核工作量随题量线性增长 | 上架速度瓶颈 | AI 生成时约束出题模板降低多样性噪音，人工审核只做抽审 + 全量机审兜底 |
| 订阅消息逐次授权 | 触达率受限 | 按 FR-B3 设计：打卡庆祝页顺带引导续订 |
| 小程序类目与备案 | 上线进度 | 按 PRD 8.3 路径执行：个体户主体 → 在线教育类目 → 微信认证 + ICP 备案 |

---

## 变更记录

### v1.1（2026-08-29）

- **组件库：TDesign（Taro React 版）→ NutUI-React-Taro（@nutui/nutui-react-taro v3.0.20）**
- 原因：TDesign 官方未提供可用的 Taro React 组件包，npm 上 `tdesign-miniprogram-taro` 仅为 0.0.4 占位包，无法落地。
- 新选型说明：NutUI 为京东官方 Taro React 组件库，与 Taro 4 + React 18 官方适配；本项目 UI 以童趣风格自绘为主，NutUI 主要承担通用交互控件（表单、弹层、日历等）。工程已按官方要求启用 `@tarojs/plugin-html`、designWidth 切换为 375 基准。
- 决策人：项目所有者（M0 确认门，2026-08-29）。
