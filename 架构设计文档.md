# 《趣狗乐学》小程序 架构设计文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 编写日期 | 2026-08-29 |
| 依据文档 | PRD v1.0（研发级）、《技术选型文档》v1.0 |
| 范围 | P0（MVP）完整架构与技术方案；P1/P2 给出演进预留，不展开细节 |

---

## 1. 总体架构

### 1.1 架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                     微信小程序（Taro + React）                │
│                                                             │
│  主包：首页 / 学习乐园 / 打印小铺Tab / 个人中心               │
│  分包：game（口算闯关、翻翻乐、花园兑换）                     │
│        print（模板/参数/预览/生成结果）                       │
│                                                             │
│  Zustand 状态层 ─ services 请求层 ─ 离线队列 / 本地缓存       │
└──────────────┬──────────────────────────────────────────────┘
               │ wx.cloud.callFunction（全部走云函数，
               │        前端不直连云数据库）
┌──────────────▼──────────────────────────────────────────────┐
│                    微信云开发 CloudBase                       │
│                                                             │
│  云函数层（8 个）：profile / task / garden / report /        │
│            print / track / cron-reminder / pipeline         │
│                                                             │
│  云数据库：users / questions / daily_tasks / daily_stats /   │
│            gardens / print_jobs / track_events              │
│                                                             │
│  云存储：插图素材库（已审核）/ 生成的 PDF 文件                │
│  定时触发器：订阅消息提醒扫描 / 内容管线分批执行              │
│  云调用：msgSecCheck / imgSecCheck / OCR / 订阅消息下发      │
└──────────────┬──────────────────────────────────────────────┘
               │
┌──────────────▼──────────────────────────────────────────────┐
│  外部能力（经云函数封装）                                     │
│  · 混元生图 API —— 仅内容生产管线（离线）使用                 │
│  · 微信同声传译插件 WechatSI —— 前端直接调用（TTS）           │
│  · 云开发 AI 网关 —— P1 预留接口，P0 不接入                  │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 核心架构原则

1. **前端不直连云数据库**：所有读写经云函数。数据库权限统一设为「仅云函数可读写」，收敛校验逻辑、缩小攻击面（儿童产品）。
2. **运行时只读内容库**：孩子使用时不触发任何 AI 生成；出题只从已审核题库抽取（选型文档 4.5 的硬约束落到架构上：生图/生文仅存在于 `pipeline` 函数）。
3. **反馈先行、落库异步**：答对动画 < 300ms 由本地先行（FR-A2），成绩异步提交；断网可离线作答，恢复联网补传（FR-A1）。
4. **按日预聚合**：报告与打卡数据在提交成绩时同步聚合进 `daily_stats` / `daily_tasks`，查询端只做简单读取，规避云数据库聚合能力弱的问题。
5. **出题能力做端口抽象**：`QuestionProvider` 接口统一出题入口，P0 实现为题库抽取（`BankQuestionProvider`），P1 替换为 AI 生成实现（`AIQuestionProvider`），升降档规则保留为兜底（FR-D1），切换不改上层代码。

## 2. 工程结构（pnpm monorepo）

```
dog-happylearn/
├── client/                      # Taro 4 + React 18 + TS 前端
│   └── src/
│       ├── app.config.ts        # 4 个 Tab + 分包配置
│       ├── pages/               # 主包页面（Tab 页必须在主包）
│       │   ├── home/            # 首页：今日任务 + 成长花园 + 快捷入口
│       │   ├── park/            # 学习乐园：游戏库 / 成就徽章
│       │   ├── print-home/      # 打印小铺 Tab 入口页（模板列表）
│       │   └── profile/         # 个人中心（报告/打卡/提醒/防沉迷入口）
│       ├── packageGame/         # 分包：口算闯关 / 翻翻乐 / 花园兑换
│       ├── packagePrint/        # 分包：参数设置 / 预览 / 生成结果
│       ├── packageReport/       # 分包：报告详情 / 打印月历 / 设置页
│       ├── components/          # 通用组件（任务卡、花园场景、庆祝动画等）
│       ├── stores/              # Zustand：profile / task / garden / screenTime
│       ├── services/            # 云函数调用封装（按域组织 + 统一错误/重试）
│       ├── utils/               # tracker / screen-time / offline-queue / tts
│       └── types/               # 与 shared 对齐的前端类型
├── cloud/
│   └── functions/               # 8 个云函数，每个独立 package.json
│       ├── profile/  task/  garden/  report/
│       ├── print/    track/ cron-reminder/ pipeline/
├── shared/                      # 前后端共享：类型 + 纯逻辑（Jest 主战场）
│   └── src/
│       ├── types/               # Question / Task / Stats / 集合 Schema 类型
│       ├── question-engine/     # 抽题规则、口算生成器、升降档规则（FR-D1/Q1/Q2）
│       └── checkin/             # streak 计算、里程碑判断（FR-B2）
├── docs/                        # PRD、选型、架构、需求/进度文档
└── package.json                 # pnpm workspace
```

升降档阈值（Q5）、单局题量（Q1）、混排比例（Q2）、牌阵规格（Q3）等开放问题收敛为 `shared` 内的**常量配置**，产品拍板后改一处即可。

## 3. 前端架构设计

### 3.1 分包与首屏性能（PRD 8.1：首屏 < 2s）

| 包 | 内容 | 说明 |
| --- | --- | --- |
| 主包 | 4 个 Tab 页 + 首页依赖组件 | 目标 < 1.5MB；首页数据由一次聚合接口返回 |
| packageGame | 口算闯关、翻翻乐、花园兑换页 | 点击任务卡时按需加载；游戏内 Lottie/CSS 动画资源随分包 |
| packagePrint | 参数/预览/生成结果页 | 模板缩略图走云存储 CDN，不进包 |
| packageReport | 报告详情/月历/设置页 | 家长低频页面，拆出主包 |

补充策略：进入首页即静默预下载当日题包（`task.ensureToday`），孩子点任务卡时零等待开局，直接服务「30 秒玩到第一题」（FR-N1）。

### 3.2 状态管理（Zustand）

| Store | 职责 | 持久化 |
| --- | --- | --- |
| profileStore | 龄段、防沉迷上限、提醒设置、streak | storage |
| taskStore | 今日任务列表、题包缓存、任务进度、离线待补传队列 | storage（当日有效，0 点清理） |
| gardenStore | 作物状态、花朵余额、宠物/场景 | 不持久化，以云端为准，本地仅动画加速 |
| screenTimeStore | 当日前台活跃累计时长、锁定状态 | storage（按日重置） |

### 3.3 关键前端机制

- **请求层**：`services/` 封装 `callFunction`，统一超时（8s）、错误码、网络异常重试（1 次）；离线时写入 `offline-queue`。
- **离线作答**（FR-A1）：题包本地缓存 → 断网可完成整局 → 成绩进待补传队列 → `onNetworkTypeChange` 恢复联网按序补传 → 云端以任务状态幂等去重。
- **防沉迷计时**（FR-B5）：App `onShow/onHide` 记录前台活跃区间，累计写 storage 并随成绩提交上报云端兜底；达上限置锁定态，仅拦截游戏入口。
- **TTS**：`utils/tts.ts` 封装 WechatSI 插件，供翻翻乐配对发音与低龄语音提示（FR-G2/FR-A3）。
- **埋点**：`utils/tracker.ts` 本地缓存批量上报（满 10 条或 30s 触发），事件 E1–E10 与 PRD 9.2 一一对应，不采集任何身份字段。

## 4. 后端架构设计（云函数）

### 4.1 云函数清单

| 函数 | 职责 | 对应需求 | 备注 |
| --- | --- | --- | --- |
| `profile` | 档案读写（龄段/提醒/防沉迷上限）、首页聚合查询（任务+花园+streak 一次返回） | FR-B3/B4/B5、FR-N1 | 换龄段次日生效在此控制 |
| `task` | 今日任务惰性生成与抽题、单局成绩提交（核心事务）、题包下发 | FR-A1/G1/G2、FR-D1 规则版 | 最核心函数 |
| `garden` | 花园详情、兑换宠物/场景 | FR-A2 | 兑换只做正向激励 |
| `report` | 今日/近 7 日报告、打卡月历、里程碑分享卡片数据 | FR-B1/B2 | 纯读，靠预聚合集合 |
| `print` | 模板列表、参数校验、PDF 生成、生成记录与埋点 | FR-C1–C4 | 高内存配置（512MB+） |
| `track` | 埋点批量入库 | 9.2 | 轻量 |
| `cron-reminder` | 定时触发（每 10 分钟）：扫描提醒时间窗口内、当日未打卡且已订阅的用户，下发订阅消息 | FR-B3 | 打卡庆祝页引导续订提升额度 |
| `pipeline` | 内容生产管线：批量出题→生图→机审→入库（离线，管理员白名单） | 选型 4.5 | 不对外暴露，生产环境白名单 openid |

云函数间不互调（避免链式冷启动）；`task` 内事务直接写花园与统计，不拆独立函数。

### 4.2 调用与权限规范

- 所有云函数返回统一结构 `{ code, msg, data }`；业务错误码集中定义在 `shared/types/errors`。
- 数据库集合权限全部关闭前端读写；`pipeline` 增加 openid 白名单校验。
- 每个云函数入口统一做参数校验（复用 shared 内的 zod schema）与结构化日志（openid 脱敏）。

## 5. 数据模型设计（云数据库集合）

| 集合 | 关键字段 | 说明 |
| --- | --- | --- |
| `users` | `_openid, age_band('4-6'|'6-8'), levels{subject:1..3}, screen_time_limit_min(默认15), reminder{enabled,time}, streak_days, best_streak, onboard_done, created_at` | 档案唯一文档。隐私约束（8.2）：**无**姓名/照片/定位字段，从模型层杜绝 |
| `questions` | `subject(oral/pinyin/poem/english), age_band, level(1..3), type(oral_calc/flashcard), knowledge_point, content(JSON 按题型), media{image_file_id, tts_text}, status(online/offline), created_at` | 内容库，仅 `pipeline` 写入、`status=online` 才可被抽出 |
| `daily_tasks` | `_openid, date('YYYY-MM-DD'), age_band, tasks[3]{seq, subject, game_type, status(pending/doing/done), question_ids, round_ids[], result{correct,total,duration_ms}}, checkin(bool), all_done_at` | 每人每日一文档，惰性生成；打卡月历直接按日期范围查此集合 |
| `daily_stats` | `_openid, date, per_subject{oral/pinyin/poem/english}{count, correct, duration_ms, wrong_points[]}, screen_time_min` | 报告数据源（FR-B1），提交成绩时 upsert；薄弱点=错误率最高知识点 |
| `gardens` | `_openid, growth(0-100 当前作物), flowers(余额), pet, scenes[], exchanged_log[]` | 花园状态，`task` 事务内更新 |
| `print_jobs` | `_openid, template, params, file_id, duration_ms, created_at` | 生成记录，H3 指标数据源 |
| `track_events` | `_openid, event(E1..E10), params, client_ts, created_at` | 仅追加 |

索引要点：`daily_tasks(_openid, date)` 唯一；`questions(subject, age_band, level, status)` 组合索引；`track_events(event, client_ts)`。

## 6. 核心技术方案

### 6.1 今日任务生成与出题引擎（FR-A1 / FR-G1 / FR-G2 / FR-D1 规则版）

```
孩子当日首次打开首页
  → task.ensureToday：
      查 daily_tasks(date=今天) 不存在则创建
      按龄段取学科组合（4-6：口算+拼音+古诗；6-8：口算+英语+古诗）
      每任务经 QuestionProvider 抽题：
        BankQuestionProvider：questions 集合按 subject+age_band+users 当前档位
        随机抽 N 题，同局去重；口算按档位混排比例控制进/退位占比
      任务写入 daily_tasks，题包下发前端缓存
```

- **难度档位**：口算 L1(10 以内)/L2(20 以内)/L3(100 以内)，初始档随龄段（5.3）；翻翻乐按卡组难度档组织。
- **升降档**（Q5 暂定）：口算连续 3 局全对 → 升一档；单局正确率 < 60% → 降一档。判断逻辑在 `shared/question-engine`，`task.submitRound` 事务内写回 `users.levels`，并在报告中向家长说明档位变化。
- **P1 演进**：新增 `AIQuestionProvider` 实现同一接口（云开发 AI 网关生文出题，仍先审后入），抽题入口不变。

### 6.2 单局成绩提交事务（FR-A1/A2/B1/B2 联动核心）

```
前端：游戏结束 → 本地先播奖励动画(<300ms) → task.submitRound(roundId, taskId, 成绩, 当日累计时长)
云端（单个事务，roundId 幂等：任务已 done 则直接返回现状）：
  1. daily_tasks.tasks[i] → done，写 result
  2. daily_stats upsert（分科目计数、错误知识点、时长）
  3. gardens.growth += 答对数；整组 done → flowers += 1
  4. 3 任务全 done → checkin=true，streak 重算（中断归 1，无补卡），里程碑(7/30/100)标记
  5. 升降档判断 → users.levels
  返回：花园增量、打卡状态、是否里程碑 → 前端据此播放开花/打卡/里程碑庆祝
```

前端本地乐观更新（任务卡状态、花园动画）与云端结果收敛：以 submitRound 返回为准，失败进离线队列重试，保证最终一致。

### 6.3 学习报告与打卡（FR-B1 / FR-B2）

- 今日报告 = `daily_tasks`（完成情况）+ `daily_stats`（分科正确率）；`report` 函数纯读拼接，文案模板（如「口算 10 题对 9 题，20 以内退位减法还需练习」）在 `shared` 内生成。
- 打卡月历 = 按 `daily_tasks.checkin` 查当月文档；streak 读 `users.streak_days`（事务内已维护），避免逐日计算。
- 里程碑（7/30/100 天）：Canvas 绘制分享卡片，`profile` 返回 streak 数据即可，图片本地生成。

### 6.4 打印小铺 PDF 生成（FR-C1–C4）

```
print.generate(params)
  1. 参数校验（shared 内 zod：FR-C2 范围，默认值即开即用）
  2. 组题：
     口算题卡 → shared 口算生成器按 运算类型/数字范围/题量 运行时生成算式
       （数学算式为规则产物非 AI 内容，无需审核；插图从已审核素材库取）
     识字卡 → 字集 = 今日所学(daily_stats) + 薄弱字，缺额从字表库补足
  3. pdfkit 渲染 A4：中文字体子集打包进函数（<2MB）、裁切线、插图装饰、
     识字卡双面排版 + 制作说明
  4. 上传云存储 print/{openid}/{jobId}.pdf → 记录 print_jobs
  5. 返回 fileID + 临时链接；前端提供 保存/转发/跳打印店（FR-C4，埋点 E5/E6）
```

失败重试幂等：`jobId` 前端生成，重试复用同一 jobId 覆盖写。内存配置 512MB 起步，生成目标 < 5s / < 5MB。

### 6.5 订阅消息提醒（FR-B3）

- 设置保存 → 前端拉起订阅授权，授权一次记录一次额度（`reminder` 状态随 `users` 存储）。
- `cron-reminder` 每 10 分钟触发：扫描 `reminder.enabled=true` 且 `time` 落在当前窗口、当日 `checkin=false` 的用户，有订阅额度则 `subscribeMessage.send`（云调用）。
- 触达率关键设计：打卡庆祝页与首次引导（FR-N1）内嵌续订引导；额度耗尽则不再发送。

### 6.6 防沉迷（FR-B5）

口径 = 小程序前台活跃时间。前端 `screenTimeStore` 计时并按日持久化；每次 `submitRound` 携带当日累计时长写入 `daily_stats.screen_time_min`（云端兜底口径）。达上限：前端置锁定态，弹温和收尾动画，仅锁游戏入口（打印小铺/个人中心不受限）；云端在 `ensureToday` 返回锁定标记，防止绕过前端。

### 6.7 内容生产管线（选型 4.5，`pipeline` 函数）

```
定时触发器分批执行（可断点续跑，失败重试不重复计费）：
  1. 生文模型批量出题（题目文案/解析/知识点标签，按「龄段×科目×难度档」题包规划）
  2. 混元生图按统一「童趣扁平风」Prompt 模板 + 固定负面词生成插图（按主题批量复用）
  3. 脚本化质量初筛 → msgSecCheck / imgSecCheck 机审 → status=pending_review
  4. 人工审核 → status=online
前端运行时只读 status=online 的内容，与生成链路完全隔离。
```

首批规模按选型文档：2 龄段 × 3–4 科目 × 3 档 × 100 题 ≈ 2000 题量级。

### 6.8 新用户激活（FR-N1）

`profile.ensure`：首次进入静默建档（openid 免鉴权，无注册）；选龄段写入 `users.age_band` → 直接跳第一局游戏（题包已预下载）→ 首局完成触发首次开花 → 庆祝页内嵌提醒设置 + 订阅授权 → 完成首次打卡（埋点 E10）。全程无强制授权弹窗。

## 7. 关键接口契约（云函数 action）

| 函数 | action | 入参要点 | 返回要点 |
| --- | --- | --- | --- |
| `profile` | `ensure` / `get` / `updateAgeBand` / `updateSettings` | 龄段、提醒时间(07:00–21:30)、时长上限(10–30) | 首页聚合：任务摘要、花园摘要、streak、锁定状态 |
| `task` | `ensureToday` | — | 3 任务 + 题包 + 当日累计时长 |
| `task` | `submitRound` | taskId, roundId, 成绩, 累计时长 | 花园增量、打卡/里程碑状态、新档位 |
| `garden` | `get` / `exchange` | 兑换对象 id | 花园全量状态 |
| `report` | `today` / `weekly` / `calendar` | 月份 | 分科正确率、薄弱点、打卡日历、streak |
| `print` | `templates` / `generate` / `jobs` | 模板 id、FR-C2 参数 | 缩略图列表 / fileID+临时链接 / 历史记录 |
| `track` | `report` | 事件数组（≤50 条/批） | — |
| `cron-reminder` | （定时触发） | — | 发送结果日志 |

字段命名与 PRD 术语表严格一致（task_type、age_band、difficulty_level 等埋点参数同名）。

## 8. 非功能设计

| 维度 | 方案 |
| --- | --- |
| 性能 | 分包加载 + 首页聚合接口 + 题包预下载（首屏 < 2s）；答对动画本地先行（< 300ms）；PDF 高内存函数 + 字体子集（< 5s / < 5MB） |
| 安全 | 前端不直连数据库；`pipeline` 白名单；云函数参数 zod 校验；日志 openid 脱敏 |
| 隐私合规 | `users` 集合从模型层不建姓名/照片/定位字段；教材图片（P1）仅即时解析不落库；埋点无身份字段 |
| 内容安全 | 题库/插图仅经管线「机审 + 人工」后置 `online`；运行时无任何生成入口 |
| 可观测 | 云函数结构化日志 + 错误码统一上报 `track_events`；免费额度水位（调用量/存储）每日 cron 检查告警 |

## 9. 测试策略

| 层次 | 工具 | 覆盖点 |
| --- | --- | --- |
| 单元测试 | Jest | `shared` 纯逻辑：抽题与混排比例、升降档（Q5）、streak 与里程碑、PDF 参数校验、报告文案生成 |
| 云函数测试 | Jest + 本地 mock 云 SDK | `submitRound` 事务各分支（幂等/打卡/streak 归零）、`ensureToday` 惰性生成与换龄段次日生效 |
| 自动化回归 | miniprogram-automator | 三条核心路径：新用户激活（FR-N1）、每日闭环（任务→花园→打卡→报告）、打印生成（FR-C1–C4） |

## 10. 环境与部署

- 环境：CloudBase `dev` / `prod` 双环境，前端按编译变量注入 env；云函数经 CloudBase CLI 部署。
- CI：`miniprogram-ci` 构建体验版；Jest 单测通过为合并前置。
- 发布顺序：内容管线先行灌库并审核 → 云函数 → 小程序提审（类目：在线教育，主体/认证/备案按 PRD 8.3 路径）。

## 11. P1/P2 演进预留

| 演进项 | 架构上的预留 | 改动面 |
| --- | --- | --- |
| FR-D1 AI 自适应出题 | `QuestionProvider` 接口新增 `AIQuestionProvider`，升降档规则降级为兜底 | 仅 `task` 函数替换实现 |
| 模块 E AI 认知乐园 | 内容复用 `questions` 集合扩展 `subject=cognition`；故事/卡片走既有内容库与翻翻乐框架 | 内容管线扩充 + packageGame 新页面 |
| 模块 F 教材同步 | `print` 旁新增 `textbook` 函数（OCR 云调用 + AI 网关），上传文件走云存储临时目录即时删除 | 新增函数 + 新分包 |
| 多孩档案 / 完整档案 | `users` 拆 `children` 子集合，`age_band` 查询处加 childId 参数 | `profile`/`task` 小改 |
| 打印素材包变现（P2） | `print_jobs` 已记录模板与参数，支付经微信支付商户号 | 新增订单函数 |

## 12. 风险与对策

沿《技术选型文档》第 6 章风险表执行，补充两条架构侧对策：

1. **云函数冷启动影响首题体验**：题包预下载 + `ensureToday` 惰性生成放在首页打开时（而非点任务卡时），冷启动被首页加载掩盖。
2. **`submitRound` 事务失败导致奖励丢失**：前端离线队列重试 + roundId 幂等，最坏情况是重复提示而非数据错乱；花园增量以云端返回为准收敛。
