# 《趣狗乐学》小程序 方案设计与开发规划

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 编写日期 | 2026-08-29 |
| 依据文档 | PRD v1.0、《技术选型文档》v1.0、《架构设计文档》v1.0 |
| 定位 | 架构设计的落地执行版：模块级实现方案 + 开发任务拆解 + 里程碑与验收标准 |
| 工作量口径 | 按 1 名全栈开发估算（人日），多人并行时可压缩关键路径，见 3.2 |

---

## 1. 开发总原则

1. **模块内先先后端、再单测、再前端、再前端测试、最后文档**（开发流程约束），每个模块完成即更新《开发进度.md》并停下确认后再进下一模块。
2. **主循环优先**：一切开发围绕 P0 最小闭环「玩 10 分钟 → 开花 → 报告 → 打印 → 明日打卡」，先打通纵向闭环再横向补齐。
3. **内容先行**：题库空转会让前端无法联调，内容管线（M1）与后端并行启动，M2 前端开工时必须有可用的种子题库。
4. **纯逻辑全部下沉 `shared`**：凡能用 Jest 覆盖的逻辑（抽题、升降档、streak、参数校验、文案生成）不写进云函数和前端组件。

## 2. 模块划分与依赖关系

```
M0 工程初始化 ──► M1 后端核心引擎 ──► M2 核心学习闭环（前端）──► M4 家长侧
        │                │                                        ▲
        │                └──► M1.5 内容管线（并行）                │
        └──► M3 打印小铺 ◄──────────── 需 M1 题库/字表 ────────────┘
                                          │
                       M5 测试与联调 ◄────┘（全部模块完成后）
                                          │
                       M6 提审上线
```

| 模块 | 内容 | 对应 PRD | 前置依赖 |
| --- | --- | --- | --- |
| M0 | monorepo / Taro 脚手架 / 云环境 / CI | — | 无 |
| M1 | shared 纯逻辑 + profile/task/garden/track 云函数 + 单测 | FR-A1、FR-D1 规则版、FR-B5 云端部分 | M0 |
| M1.5 | 内容生产管线 + 种子题库（与 M1 并行） | 选型 4.5 | M0 |
| M2 | 核心学习闭环前端：首页/口算/翻翻乐/花园/打卡庆祝 | FR-A1/A2/G1/G2、FR-N1 前半 | M1、M1.5 |
| M3 | 打印小铺前后端 | FR-C1–C4 | M1（题库/字表）、M2（复用组件） |
| M4 | 家长侧：报告/月历/提醒/防沉迷/新用户引导 | FR-B1–B5、FR-N1 | M2 |
| M5 | 埋点全量、automator 回归、真机与性能、双端联调 | 9.2、8.1 | M2–M4 |
| M6 | 合规素材、提审、体验版发布 | 8.3 | M5 |

每个模块完成后设**确认门**：演示 + 文档更新 + 用户确认，再进入下一模块。

## 3. 里程碑计划

### 3.1 里程碑总表

| 里程碑 | 交付物 | 验收门槛 | 预估 |
| --- | --- | --- | --- |
| M0 完成 | 可运行的空壳小程序 + dev 云环境 + CI 构建 | `pnpm dev:weapp` 编译通过、4 个空 Tab 可打开、云函数 hello world 通 | 3 人日 |
| M1 完成 | 核心云函数 + 单测 + 接口联调文档 | Jest 覆盖 shared 全部纯逻辑且通过；云函数本地/云端测试通过 | 6 人日 |
| M1.5 完成 | 内容库 ≥ 2000 题在线 | 每龄段×科目×档位题量达标、全部 status=online、插图风格抽检一致 | 5 人日（并行） |
| M2 完成 | 可玩的每日闭环 demo | 新设备 30 秒内玩到第一题；3 任务 → 开花 → 打卡全链路真机可走通 | 8 人日 |
| M3 完成 | 打印闭环可用 | 任一模板 5 秒内出真 PDF（<5MB），保存/转发成功 | 5 人日 |
| M4 完成 | P0 全功能完整 | 家长侧四件套（报告/月历/提醒/防沉迷）+ 新用户激活路径真机走通 | 7 人日 |
| M5 完成 | 测试报告 + 性能达标 | automator 三路径全绿；首屏 <2s、答对反馈 <300ms 实测达标 | 4 人日 |
| M6 完成 | 提审通过、体验版发布 | 类目/认证/备案就绪，审核反馈问题清零 | 3 人日（含等待） |

合计约 33 人日（含 M1.5 并行），即单人约 6–7 周日历时间（含提审等待）。

### 3.2 并行策略（多人时）

- 后端线：M0 → M1 → M3 后端 → M4 后端；
- 前端线：M0 → M2 → M3 前端 → M4 前端；
- 内容线（可外包/半自动）：M1.5 与前端线全程并行，M2 开工前交付首批 300 题种子包即可启动联调。

## 4. 各模块实现方案与任务拆解

### M0 工程初始化（3 人日）

| # | 任务 | 产出 | 验收标准 |
| --- | --- | --- | --- |
| 0.1 | pnpm monorepo 脚手架 | client / cloud / shared 三包 + workspace 配置 | 目录结构与架构文档 §2 一致 |
| 0.2 | Taro 4 + React + TS 初始化 | 4 Tab 空页 + 三分包骨架 + app.config | 编译通过、真机预览可打开 |
| 0.3 | TDesign + Zustand + Lottie 接入 | 组件库按需引入、stores 骨架、请求层封装雏形 | demo 页可调通一次 callFunction |
| 0.4 | 云开发 dev 环境 + 首个云函数部署链路 | CloudBase CLI 部署脚本、集合权限统一配置 | 云端函数可被小程序调用 |
| 0.5 | 规范与 CI | ESLint/Prettier、Conventional Commits、miniprogram-ci 体验版构建脚本 | 提交触发 lint + 构建 |

### M1 后端核心引擎（6 人日）

**实现顺序（先纯逻辑、后接口）：**

| # | 任务 | 内容要点 | 验收标准 |
| --- | --- | --- | --- |
| 1.1 | `shared/types` | 全部集合 Schema、云函数出入参、错误码枚举 | 前后端共用，tsc 零错误 |
| 1.2 | `shared/question-engine` | 抽题（去重/混排比例 Q2）、口算生成器、升降档规则（Q5） | Jest 覆盖边界：题不足、档位顶/底、连续全对/低正确率 |
| 1.3 | `shared/checkin` | streak 计算（中断归 1、无补卡）、里程碑 7/30/100 | Jest 覆盖跨月、断签、重复提交 |
| 1.4 | `profile` 云函数 | ensure / get（首页聚合）/ updateAgeBand（次日生效）/ updateSettings | 接口文档 + 云端实测 |
| 1.5 | `task` 云函数 | ensureToday（惰性生成+抽题+预下载）、submitRound（§4.1 事务） | 幂等/事务分支单测 + 云端实测 |
| 1.6 | `garden` / `track` 云函数 | 花园 get/exchange；埋点批量入库 | 云端实测 |
| 1.7 | 接口联调文档 | 各 action 请求/响应字段明细表（本文件 §6 初版） | 前端可照表 mock 联调 |

**§4.1 `submitRound` 事务规格**（M1 最关键实现，分支必须全部单测）：

```
入参：taskId, roundId(前端生成 uuid), subject, correct, total, durationMs, wrongPoints[], screenTimeMin
前置校验：daily_tasks 属于 openid + 当日；tasks[i].status != done（否则幂等返回现状）
事务内：
  a. tasks[i] → done + result
  b. daily_stats upsert（分科计数/错误点/时长）
  c. gardens.growth += correct；本任务首次 done → flowers += 1
  d. 全部 3 任务 done → checkin=true；streak 重算写 users；里程碑标记
  e. 升降档判断 → users.levels（口算科目）
返回：{ gardenDelta, checkin, milestone, newLevel }
错误码：TASK_NOT_FOUND / TASK_ALREADY_DONE(幂等成功) / PARAM_INVALID / SYSTEM_ERROR
```

### M1.5 内容生产管线（5 人日，并行）

| # | 任务 | 内容要点 | 验收标准 |
| --- | --- | --- | --- |
| 1.5.1 | 管线脚本框架 | pipeline 函数 + 定时触发分批 + 断点续跑 + 失败重试不重复计费 | 中断后可续跑 |
| 1.5.2 | 生文出题批次 | 按龄段×科目×档位生成题目文案/解析/知识点标签 | 模板约束多样性噪音 |
| 1.5.3 | 生图批次 | 混元生图统一 Prompt 模板 + 负面词，按主题复用 | 废片率 < 30%（初筛后） |
| 1.5.4 | 审核入库 | 脚本初筛 → msgSecCheck/imgSecCheck → status=pending_review → 人工审核置 online | 无未审内容进 online |
| 1.5.5 | 种子包交付 | 首批 300 题（M2 联调最低需求）→ 全量 2000 题 | 每组合抽检通过 |

### M2 核心学习闭环前端（8 人日）

| # | 任务 | 内容要点 | 验收标准 |
| --- | --- | --- | --- |
| 2.1 | 首页框架 | 今日任务三卡（待开始/进行中/已完成）、花园常驻区、快捷入口（FR-A3 含每日一练预填）、进度指示 | 与原型图 p02 一致 |
| 2.2 | 龄段选择 + 首局直达 | p01 两张大卡 → 写档案 → 直接进第一局（题包预下载零等待） | 30 秒内玩到第一题 |
| 2.3 | 口算闯关 | 逐题、大按钮数字键盘、对错反馈动画、成绩结算；不限时（FR-G1） | 与原型 p05 交互一致；单局 ≤4 分钟 |
| 2.4 | 卡片翻翻乐 | 4 对 8 张（Q3）、配对成功 TTS 朗读（WechatSI）、4–6 岁提示模式默认开（Q6） | 与原型 p06 一致；三门学科卡组可配 |
| 2.5 | 成长花园 | 答对茎叶生长动画（本地先行 <300ms）、开花/打卡庆祝、兑换页（1 作物/1 宠物/2 场景） | 动画反馈 <300ms 实测；状态跨日保留 |
| 2.6 | 作答提交与离线 | submitRound 对接、乐观更新收敛、离线队列补传 | 断网整局可玩、联网自动补传、重复提交不重复奖励 |
| 2.7 | 前端自测 | 三分包真机走通每日闭环 | 自测问题清零后进确认门 |

### M3 打印小铺（5 人日）

| # | 任务 | 内容要点 | 验收标准 |
| --- | --- | --- | --- |
| 3.1 | print 云函数 | 参数校验（FR-C2 范围）、口算生成器组题、识字卡字集（今日所学+薄弱字）、pdfkit A4 渲染（字体子集/裁切线/双面识字卡/制作说明）、云存储上传 | 生成 <5s、<5MB；参数边界单测 |
| 3.2 | 模板列表页 | 大卡片真实 A4 缩略图（云存储 CDN）、龄段标注 | 与原型 p10 一致 |
| 3.3 | 参数 + 预览 + 结果页 | 大按钮分段选择器（无下拉框）、分页缩略预览、生成加载动画、保存/转发/跳打印店（E5/E6 埋点） | 与原型 p11/p12 一致；首页/报告入口预填参数生效 |
| 3.4 | 前端自测 | 双模板 × 参数组合抽测 | 确认门 |

### M4 家长侧（7 人日）

| # | 任务 | 内容要点 | 验收标准 |
| --- | --- | --- | --- |
| 4.1 | report 云函数 | today/weekly/calendar 三 action（纯读预聚合集合）、报告文案生成（shared） | 数据与 daily_stats 一致 |
| 4.2 | 学习报告页 | 数据卡片+大数字、平实文案、「打印今日练习单」按钮预填薄弱科目（FR-B1） | 与原型 p08 一致 |
| 4.3 | 打卡月历 | 脚印标记、当日状态置顶、streak 双处展示、里程碑 Canvas 分享卡片（FR-B2） | 与原型 p09 一致；断签归 1 |
| 4.4 | 提醒设置 + cron-reminder | 时间选择器（07:00–21:30 默认 19:30）、订阅授权引导、定时扫描下发；庆祝页续订引导（FR-B3） | 真机收到订阅消息；额度耗尽不发送 |
| 4.5 | 防沉迷 | 前台活跃计时、达上限温和锁定（仅锁游戏入口）、云端 ensureToday 兜底（FR-B5） | 上限 10–30 可调；切后台不计时 |
| 4.6 | 新用户激活串接 | ensure 静默建档 → 选龄段 → 首局 → 首次开花 → 设提醒 → 首次打卡（E10） | 全程无强制授权弹窗 |
| 4.7 | 前端自测 | 家长侧四件套真机走通 | 确认门 |

### M5 测试与联调（4 人日）

| # | 任务 | 验收标准 |
| --- | --- | --- |
| 5.1 | automator 三路径回归 | 新用户激活 / 每日闭环 / 打印生成 全绿 |
| 5.2 | 性能实测 | 首屏 <2s、答对反馈 <300ms、PDF <5s/<5MB、游戏 60fps |
| 5.3 | 埋点验收 | E1–E10 全部落库、参数与 PRD 9.2 一致、无身份字段 |
| 5.4 | 异常场景 | 断网作答补传、杀进程恢复、换龄段次日生效、双机同账号并发提交 |

### M6 提审上线（3 人日 + 等待）

| # | 任务 | 验收标准 |
| --- | --- | --- |
| 6.1 | 平台合规 | 个体户主体 → 在线教育类目 → 微信认证 + ICP 备案（与开发并行推进，不占关键路径） |
| 6.2 | 隐私与内容合规检查 | 隐私政策入口前置、用户信息收集清单申报、题库全量机审记录留存 |
| 6.3 | 提审与反馈修复 | 审核通过，体验版发布种子用户（100–300 家庭） |

## 5. 页面与组件清单

### 5.1 页面路由

| 路由 | 包 | 对应原型 | 模块 |
| --- | --- | --- | --- |
| pages/home | 主包 | p02 | M2 |
| pages/onboarding（龄段选择） | 主包 | p01 | M2 |
| pages/park | 主包 | — | M2 |
| pages/print-home | 主包 | p10 | M3 |
| pages/profile | 主包 | p13 | M4 |
| packageGame/pages/oral | game | p05 | M2 |
| packageGame/pages/flashcard | game | p06 | M2 |
| packageGame/pages/garden-exchange | game | p07a/b | M2 |
| packagePrint/pages/params | print | p11 | M3 |
| packagePrint/pages/preview | print | p12 | M3 |
| packagePrint/pages/done | print | p12 | M3 |
| packageReport/pages/report | report | p08 | M4 |
| packageReport/pages/calendar | report | p09 | M4 |
| packageReport/pages/settings | report | p14 | M4 |

### 5.2 核心复用组件

| 组件 | 使用方 | 说明 |
| --- | --- | --- |
| TaskCard | 首页 | 三态任务卡 + 进度 |
| GardenScene | 首页/兑换 | 花园场景渲染（CSS + Lottie） |
| CelebrateOverlay | 游戏页/首页 | 开花/打卡/里程碑全屏庆祝（粒子 + 音效） |
| StarFeedback | 口算/翻翻乐 | 答对星星粒子 + 音效、答错抖动高亮 |
| SectionPicker | 打印参数/设置 | 大按钮分段选择器（儿童可操作，无下拉框） |
| ConfirmBar | 各流程末页 | 统一主行动按钮 |

## 6. 云函数接口规格（实现级明细）

通用返回 `{ code: 0, msg: '', data }`；错误码：`PARAM_INVALID(1001) / TASK_NOT_FOUND(2001) / TASK_ALREADY_DONE(2002) / EXCHANGE_FAILED(3001) / PRINT_PARAM_INVALID(4001) / PRINT_GENERATE_FAILED(4002) / FORBIDDEN(9001) / SYSTEM_ERROR(9999)`。

| 函数.action | 请求字段 | 响应字段 |
| --- | --- | --- |
| profile.ensure | — | { openid, isNew, ageBand } |
| profile.get | — | { ageBand, levels, reminder, screenTimeLimit, streakDays, bestStreak, onboardDone, lockState } |
| profile.updateAgeBand | ageBand | { effectiveDate }（次日生效） |
| profile.updateSettings | reminder{enabled,time}?, screenTimeLimitMin? | { saved } |
| task.ensureToday | — | { date, tasks[3]{taskId,seq,subject,gameType,status,result}, questionPack, screenTimeMin, lockState } |
| task.submitRound | taskId, roundId, subject, correct, total, durationMs, wrongPoints[], screenTimeMin | { gardenDelta{growth,flowers}, checkin, milestone?, newLevel? } |
| garden.get | — | { growth, flowers, pet, scenes[] } |
| garden.exchange | itemId | { garden 全量 } |
| report.today | — | { perSubject{count,correct,durationMs}, weakPoints[], streakDays, levelNotes[] } |
| report.weekly | — | { daily[{date,rate}], weakTop3[] } |
| report.calendar | month('YYYY-MM') | { days[{date,checkin}], streakDays } |
| print.templates | — | { templates[{id,name,previewUrl,ageBands}] } |
| print.generate | templateId, params(按 FR-C2), jobId | { fileId, tempUrl, durationMs, pages } |
| print.jobs | — | { jobs[{jobId,template,createdAt,fileId}] } |
| track.report | events[{event,params,clientTs}]（≤50） | { accepted } |

## 7. 风险与应对（执行侧）

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 内容管线废片率/审核积压拖累 M2 联调 | 前端阻塞 | 种子包 300 题优先交付；插图按主题复用降低生成量 |
| pdfkit 中文排版调试超预期 | M3 延期 | M1 期间先用最小 PDF demo 验证字体子集方案（提前到 0.4 一并做） |
| 事务分支遗漏导致奖励错乱 | 数据可信度 | §4.1 分支全部 Jest 化为准入条件，未达标不进 M2 |
| 订阅消息触达率低 | H 系列假设验证失真 | 庆祝页续订引导在 M2 就实现，不等 M4 |
| 提审类目/备案等待期 | 上线延期 | 6.1 与 M0 并行启动，主体注册第一周即提交 |

---

## 附：文档关系与维护

- 本文档与《开发进度.md》配套：每个任务完成后更新进度表，模块确认门处记录确认结论；
- 需求变更时先改 PRD/架构文档，再同步本文件的模块拆解与接口规格，保持三层文档一致。
